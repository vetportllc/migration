const mongoose = require("mongoose");
const Schema = mongoose.Schema;

const plan_action = new Schema(
  {
    id: {
      type: String,
      trim: true,
    },
    recordID: {
      type: String,
      trim: true,
    },
  },
  { timestamps: true, strict: false }
);

module.exports = mongoose.model("plan_action", plan_action);
